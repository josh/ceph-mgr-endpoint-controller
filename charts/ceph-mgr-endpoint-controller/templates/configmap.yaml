{{- if .Values.config.create }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.config.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ceph-mgr-endpoint-controller.labels" . | nindent 4 }}
data:
  ceph.conf: |
    [global]
    {{- if .Values.config.clusterID }}
    fsid = {{ .Values.config.clusterID }}
    {{- end }}
    {{- if .Values.config.monitors }}
    mon_host = {{ join "," .Values.config.monitors }}
    {{- end }}
{{- end }}
